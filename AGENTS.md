# Project Instructions

- Keep this project synchronized with its GitHub repository.
- For future work in this project, commit and push completed changes to the GitHub repo unless explicitly asked not to.
- Prefer relying on Vercel's Git integration to auto-deploy pushed changes. Use direct Vercel deploys only when explicitly requested or when Git-backed deployment is blocked.
